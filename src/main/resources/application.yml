spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/secure_file_sharing
    username: postgres
    password: has1hdfasdf1ka1ssa

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB